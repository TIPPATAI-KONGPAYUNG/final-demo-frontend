# 🤖 Blockly Adventure Game

เกมการเรียนรู้การเขียนโปรแกรมด้วย Blockly และ Phaser.js ที่มีการแยกส่วนข้อมูลและ components อย่างเป็นระเบียบ

## 🏗️ โครงสร้างโปรเจค

### 📁 โฟลเดอร์หลัก
```
src/
├── data/           # Mock Data สำหรับจำลอง Database
├── utils/          # Utility Functions
├── components/     # React Components
├── pages/          # หน้าเว็บแอปพลิเคชัน
└── App.jsx         # ไฟล์หลักของแอป
```

### 📊 Mock Data (`src/data/mockData.js`)
- **PREDEFINED_WEAPONS**: ข้อมูลอาวุธต่างๆ
- **LEVELS_DATA**: ข้อมูลด่านเกม (3 ด่าน)
- **BLOCKS_DATA**: ข้อมูล blocks สำหรับ Blockly
- **mockAPI**: ฟังก์ชันจำลอง API calls

### 🛠️ Utilities (`src/utils/`)
- **gameUtils.js**: ฟังก์ชันเกมและ game state management
- **phaserGame.js**: ฟังก์ชัน Phaser game rendering
- **blocklyUtils.js**: ฟังก์ชัน Blockly workspace และ blocks

### 🧩 Components (`src/components/`)
- **GameArea.jsx**: พื้นที่แสดงเกม Phaser
- **BlocklyArea.jsx**: พื้นที่ Blockly workspace
- **PatternManager.jsx**: จัดการ patterns และอาวุธ
- **AdminPanel.jsx**: Panel สำหรับจัดการ blocks
- **BlocklyPhaserGame.jsx**: Component หลักของเกม

### 📄 Pages (`src/pages/`)
- **AdminPage.jsx**: หน้า Admin สำหรับจัดการด่านและข้อมูล

## 🎮 ฟีเจอร์หลัก

### 🎯 ระบบด่าน
- **ด่าน 1**: พื้นฐาน - เรียนรู้การเคลื่อนที่
- **ด่าน 2**: หลบหลีก - เรียนรู้การหลบหลีกอุปสรรค
- **ด่าน 3**: ลูปและเงื่อนไข - เรียนรู้การใช้ลูปและเงื่อนไข

### ⚔️ ระบบอาวุธและ Pattern
- อาวุธมี Power ต่างกัน
- Pattern ตรง = อาวุธดี (ไม่โดน damage)
- Pattern ไม่ตรง = อาวุธแย่ (โดน damage)

### 🧩 Blocks ที่รองรับ
- **การเคลื่อนที่**: เดินไปข้างหน้า, หันซ้าย, หันขวา, โจมตี
- **ตรรกะ**: if-else, if only
- **เงื่อนไข**: เจอ Monster, เดินต่อได้, ใกล้หลุม, ถึงเป้าหมาย
- **ลูป**: ทำซ้ำ, ทำซ้ำจนกว่า

### ⚙️ ระบบ Admin
- จัดการ blocks ที่เปิด/ปิดในแต่ละด่าน
- จัดการ patterns และอาวุธ
- ดูข้อมูลสถิติของด่าน

## 🚀 การใช้งาน

### หน้าเกม (`/`)
1. เลือกด่านที่ต้องการเล่น
2. ลาก blocks มาต่อกันใน Blockly workspace
3. กดปุ่ม "เริ่มเกม" เพื่อรันโค้ด
4. ดูผลลัพธ์ในเกม Phaser

### หน้า Admin (`/admin`)
1. เลือกด่านที่ต้องการจัดการ
2. เปิด/ปิด blocks ที่ต้องการ
3. เพิ่ม/ลบ patterns
4. ดูข้อมูลสถิติของด่าน

## 🛠️ การติดตั้งและรัน

```bash
# ติดตั้ง dependencies
npm install

# รันโปรเจค
npm run dev
```

## 📚 Dependencies หลัก

- **React**: UI Framework
- **Phaser**: Game Engine
- **Blockly**: Visual Programming
- **React Router**: Navigation
- **Tailwind CSS**: Styling

## 🎯 เป้าหมายการพัฒนา

1. ✅ แยกข้อมูลออกเป็นไฟล์ mock data
2. ✅ แยก components ตามหมวดหมู่
3. ✅ สร้างหน้า Admin สำหรับจัดการด่าน
4. ✅ ระบบ routing ระหว่างหน้าเกมและ Admin
5. ✅ โครงสร้างโปรเจคที่เป็นระเบียบและ maintainable

## 🔮 การพัฒนาต่อ

- เพิ่มด่านใหม่
- เพิ่ม blocks ใหม่
- ระบบ Database จริง
- ระบบ Authentication
- ระบบ Leaderboard
- Multiplayer support
